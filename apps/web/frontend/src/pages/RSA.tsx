import React from "react";

const RSAPage = () => (
  <div>
    <h1>RSA Algorithm</h1>
    <p>This page covers the RSA asymmetric encryption implementation and usage in krypto_lib.</p>
  </div>
);

export default RSAPage;
