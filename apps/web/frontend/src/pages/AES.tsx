import React from "react";

const AESPage = () => (
  <div>
    <h1>AES (Advanced Encryption Standard)</h1>
    <p>This page covers the AES block cipher implementation and usage in krypto_lib.</p>
  </div>
);

export default AESPage;
